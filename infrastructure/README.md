# infrastructure

Reserved for future infrastructure-as-code (production deployment configs,
CDN/edge config, etc.). Local development infrastructure lives in the
repo-root `docker-compose.yml` instead — see the main `README.md` for setup.

Empty in Phase 1 by design.
