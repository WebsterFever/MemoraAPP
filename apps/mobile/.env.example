# Copy to .env and adjust as needed. Never commit the real .env file.
# Expo only exposes variables prefixed EXPO_PUBLIC_ to client code — never put
# secrets here. All AI/provider calls go through the backend (see
# docs/architecture/07-security-privacy-deletion.md).

EXPO_PUBLIC_API_URL=http://localhost:3000
